# Force LF line endings for all shell scripts and Python files.
# Windows Git defaults to CRLF which breaks shebangs (#!/bin/bash\r → "No such file or directory").
*.sh    text eol=lf
*.py    text eol=lf
*.json  text eol=lf
*.md    text eol=lf
